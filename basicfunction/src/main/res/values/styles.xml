<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="custom_dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- Dialog的windowFrame框为无 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否漂现在activity上 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 是否半透明 -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 去除黑色边框的关键设置项 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 屏幕背景是否变暗 -->
        <item name="android:backgroundDimAmount">0.8</item>
    </style>

    <!--popup动画-->
    <style name="Popup_Animation_UpDown" mce_bogus="1" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/enter_dropup</item>
        <item name="android:windowExitAnimation">@anim/exit_dropdown</item>
    </style>
    <!--popup动画2-->
    <style name="Popup_Animation_scale" mce_bogus="1" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/in_like_drop_down</item>
        <item name="android:windowExitAnimation">@anim/out_like_shrink_on</item>
    </style>
    <!--popup动画2-->
    <style name="Popup_Animation_RightInOut" mce_bogus="1" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/push_left_in</item>
        <item name="android:windowExitAnimation">@anim/push_right_out</item>
    </style>

</resources>